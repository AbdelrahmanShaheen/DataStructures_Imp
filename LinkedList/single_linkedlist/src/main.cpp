#include "Single_LinkedList.cpp"

using namespace std;

int main() {
    insert_end(5);
    insert_end(10);
    insert_end(220);
    display();
    insert_nth(460, 2);
    display();
    insert_nth(600, 4);
    display();
}
